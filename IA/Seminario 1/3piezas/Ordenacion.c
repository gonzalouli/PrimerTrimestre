

void ordenar(Lista *a){

for(int i=0; i<a->ListaMax) ///tecnica burbujeo
      burbujeo(a,i,a->ListaMax)
}

void burbujeo(lista *a, int i, int n){
      int aux;
      )
      for(int k=n ; k<i ; k--){
      if(heuristicamalcolocadas(a->tElemento[k] < heuristicamalcolocadas(a->tElemento[k-1])
        aux=heuristicamalcolocadas(a->tElemento[k]);
        heuristicamalcolocadas(a->tElemento[k]=heuristicamalcolocadas(a->tElemento[k-1];
        heuristicamalcolocadas(a->tElemento[k-1]=aux;
      }
}
///implementado par aun tElemento, se debearia realizar para la funcion de evaluacion








}
